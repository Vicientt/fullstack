{
  "name": "3b-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build:deploy": "cd frontend && npm install && npm run build && cp -r dist ../backend/",
    "start:deploy": "cd backend && npm install && node index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
